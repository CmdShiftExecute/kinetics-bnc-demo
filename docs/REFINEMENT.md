# Project Intelligence refinement, 15 September 2026

## Design and implementation plan
Audience: engineering-group directors and readers with varied dashboard confidence. Voice: precise, assured, industrial. Preserve Archivo Black, JetBrains Mono, rules and purposeful motion.

Considered: enlarging the existing six equal cards would preserve familiarity but leave management priorities buried; putting exceptions first would obscure register scale; selected a three-answer overview with a supporting coverage/activity band and explicit management attention before the existing chart.

1. Reuse finalized MIS circular Module/Theme menus and exact semantic palettes. Persist PIS theme per origin before first paint. Keep module names inside menus and reporting context visible. Test real focus, activation and computed circles.
2. Reorder the existing six overview answers: register, owned project value, chase shortlist as primary; ownership coverage, open pairs, current-year order pairs as supporting. Surface no-owner count and overloaded books using existing published values and drills. Keep all six IDs and precise values.
3. Keep sector-stage visual, sector exposure, vertical ownership, top twenty, full pair funnel and unique-project explanation. Make project value semantics explicit. Improve shared reading sizes and responsive access without changing data/filter predicates.
4. Resolve every theme-sensitive literal, including grade/value heatmaps, stage marks and readboxes. Preserve stable categories and text contrast. Extend contrast gate to all three palettes, with the same numerical floors.
5. Run build, lint, data reconciliation and byte stability in this worktree. Run existing interaction negative controls plus suite/theme/anchor/browser gates and same-host performance. Inspect three themes at desktop/laptop/phone widths; deliver screenshots and validated read-only review packet.

## Analytical preservation map
| Existing answer / interaction | Refined location / guarantee |
|---|---|
| Six overview figures and exact drills | Same six IDs; three primary and three supporting readings |
| Register scale, units, date, 80 matrix rows | Page context, register figure, supporting text and Data basis |
| Owned count and full project value | Primary value plus supporting coverage; no sum across verticals |
| Pair counts, year and full funnel | Supporting activity figures, unchanged filters and full activity section |
| Chase ranking, door, owner, score, completion | Primary shortlist total; unchanged twenty-row table and definitions |
| Sector-stage count/value/share and sector summary | Unchanged chart engine, controls, tables and keyboard/pointer behavior |
| Vertical assignment, engineer books and capacity | Unchanged ten-vertical table and books, surfaced capacity attention link |
| Relevance matrix and grade/value scales | All rows, tints and cell drills retained; theme-specific semantic ramps |
| Register filters, URL/chips, sorting, columns, CSV, virtualization | Shared predicates and table retained, tested against fixtures |
| Parties, absence, vertical rankings, project/engineer drills | All fields and relationships retained; no synthetic ownership invented |
| Validation and deliberate corrupt fixtures | Existing validation and negative controls retained |

## Runtime boundary
Original main and nginx port 928 remain the source/build baseline. Review runs from the isolated refinement build. Backups under the task artifact directory remain until explicit deletion approval. Promotion is a separate final approval after the reviewable build exists.

## Adversarial repairs after independent Astra review

The user replaced the unavailable Claude review with an independent read-only Astra assessment. Five inherited defects were reproduced and repaired: ownership rows now drill by assigned vertical (`ov`), chase expands to all612 qualifying projects (`chase=1`) while preserving the exact top20, no-relationship totals retain182 firms with separate110/72 links and disclose repeated project exposure, the party picker gives the list its actual remaining viewport height, and repeated section-link activation scrolls again. Search-only URL changes retain position. No data fixture or generator rule changed.

The hostname preview was an additional delivery defect: IP checks had passed while Vite rejected the hostname. The preview now explicitly allows node-ss.tail640a1e.ts.net, and the suite gate defaults to that actual HTTP4182 address. HTTPS928 publication still waits for user confirmation.

Run `bun scripts/adversarial.ts` against the served build for exact ownership and chase CSV populations, independent fixture expectations, exclusion controls, removable chips, relationship destinations, last-row reachability and repeated anchors. `OUT` selects evidence. Existing interaction and three-theme gates remain in force.
